ARG BASE_IMAGE=sub2api-local:basecache
FROM ${BASE_IMAGE}

COPY --chown=sub2api:sub2api output/sub2api-hotfix /app/sub2api
COPY --chown=sub2api:sub2api backend/resources /app/resources

RUN chown -R sub2api:sub2api /app && chmod +x /app/sub2api
